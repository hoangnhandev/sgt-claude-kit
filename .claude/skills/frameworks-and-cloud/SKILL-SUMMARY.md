# Frameworks (Cheatsheet)

Full: `.claude/skills/frameworks-and-cloud/SKILL.md`

## 🐘 Laravel

`UserController`, `FormRequest`, `Eloquent`, `Job`.
Rule: Validate inputs, decouple logic (Service/Event).

## ☕ Spring Boot

`@Service`, `@Repository`, `DTO`, `Lombok`.
Rule: Interface-based services, Global Exception Handler.

## ⚡ Next.js/Node

`App Router`, `Server Components`, `PPR`, `Suspense`.
Rule: `use client` at leaves. `Suspense` for blocking data. Validated inputs.

## ☁️ AWS

`Lambda`, `DynamoDB`, `IAM`, `IaC`.
Rule: Least Privilege. No secrets in code.
