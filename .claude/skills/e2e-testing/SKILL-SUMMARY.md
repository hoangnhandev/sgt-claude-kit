# E2E Testing (Cheatsheet)

Full: `.claude/skills/e2e-testing/SKILL.md`

## 🛠️ Commands

`navigate`, `click`, `type`, `screenshot` (MUST), `expect_response`.

## 🔄 Flow

Navigate -> Action -> Wait API -> Screenshot -> Assert.

## ⚠️ Rules

1. Use `data-testid`.
2. Wait for API response before screenshot.
3. Test Mobile Layout.
