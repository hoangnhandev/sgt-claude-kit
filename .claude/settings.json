{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(echo '$TOOL_INPUT' | jq -r '.TargetFile // .path // empty'); if [ -n \"$FILE_PATH\" ] && [ -f \"$FILE_PATH\" ]; then npx -y prettier --write \"$FILE_PATH\" || true; fi"
          }
        ]
      }
    ]
  }
}
